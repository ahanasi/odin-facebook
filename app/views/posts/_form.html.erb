<%= form_with(model: @post, id: "new-post-form") do |form| %>

  <div class="field">
    <div class="control">
      <%= form.text_area :content, required: true, id: 'post-input', class: 'textarea p-0 mb-5', rows: "5", placeholder: "What's on your mind, #{current_user.name}?" %>
    </div> 
  </div>
<% end %>
